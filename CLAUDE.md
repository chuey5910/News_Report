# วิธีทำงานกับเจ้าของโปรเจกต์นี้ (สำคัญ — อ่านก่อนตอบทุกครั้ง)

## การสื่อสาร

- **บอกทีละขั้นตอนในแชทเท่านั้น** ให้คำสั่งที่ต้องพิมพ์มาทีละขั้น รอผลลัพธ์ แล้วค่อยบอกขั้นต่อไป
- **ห้ามเขียนคู่มือเป็นไฟล์ .md ให้ไปอ่านเอง** เจ้าของไม่อ่านไฟล์คู่มือ — ถ้าจำเป็นต้องมีไฟล์ก็เขียนได้
  แต่**ห้ามชี้ให้ไปอ่านแทนการบอกในแชท** ต้องบอกขั้นตอนในแชทอยู่ดี
- **ห้ามให้ชุดคำสั่งยาวๆ หลายขั้นพร้อมกัน** และห้ามให้ทางแยก "ถ้าเจอ ก ทำแบบนี้ / ถ้าเจอ ข ทำแบบนั้น"
  ให้ถามผลลัพธ์แล้วตัดสินใจให้เอง
- ตอบสั้น ตรงประเด็น ภาษาไทย ไม่ต้องอธิบายทฤษฎีหรือเหตุผลยาว ถ้าไม่ได้ถาม
- เจ้าของไม่ใช่โปรแกรมเมอร์ — บอกให้ชัดว่า **พิมพ์ที่เครื่องไหน** และ **ควรเห็นผลอะไร**

## สภาพแวดล้อมจริง

- Mac mini (ที่ทำงาน) — เครื่องที่รันระบบอยู่ปัจจุบัน โฟลเดอร์ `/Volumes/CHUEY-Server/News_Report`
  รีโมทเข้าไปทำงานผ่าน Terminal ของเครื่องนี้ได้ (ใช้ MacBook เป็นจอรีโมท)
- NAS UGREEN DXP4800 Plus (บ้าน) — `192.168.1.51` ปลายทางที่จะย้ายระบบไป มี Tailscale ใน Docker
- เข้าถึงระบบจากมือถือผ่าน Tailscale เท่านั้น ไม่เปิดออกอินเทอร์เน็ต
- แจ้งเตือนเข้า LINE OA แบบส่งเข้ากลุ่ม (โควตาฟรี 300 ข้อความ/เดือน)

## ข้อห้าม

- ห้ามขอให้เจ้าของส่งเนื้อหาไฟล์ `.env` มาให้ดู (มี token จริง)
- ห้าม push ไปสาขาอื่นนอกจาก `claude/news-reporting-app-mvo1r6`
